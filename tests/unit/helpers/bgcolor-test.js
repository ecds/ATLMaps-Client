import { bgcolor } from '../../../helpers/bgcolor';
import { module, test } from 'qunit';

module('Unit | Helper | bgcolor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bgcolor(42);
  assert.ok(result);
});
