import {
  isCollaborator
} from '../../../helpers/is-collaborator';
import { module, test } from 'qunit';

module('IsCollaboratorHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isCollaborator(42);
  assert.ok(result);
});
