import { showBrowse } from '../../../helpers/show-browse';
import { module, test } from 'qunit';

module('Unit | Helper | show browse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showBrowse(42);
  assert.ok(result);
});
