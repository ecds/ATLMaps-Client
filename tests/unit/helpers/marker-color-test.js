import { markerColor } from '../../../helpers/marker-color';
import { module, test } from 'qunit';

module('Unit | Helper | marker color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = markerColor(42);
  assert.ok(result);
});
