import { hasResults } from '../../../helpers/has-results';
import { module, test } from 'qunit';

module('Unit | Helper | has results');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasResults(42);
  assert.ok(result);
});
