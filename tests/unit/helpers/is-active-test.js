import {
  isActive
} from '../../../helpers/is-active';
import { module, test } from 'qunit';

module('IsActiveHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isActive(42);
  assert.ok(result);
});
