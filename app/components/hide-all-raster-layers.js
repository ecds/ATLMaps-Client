import Ember from 'ember';

export default Ember.Component.extend({
	didInsertElement: function(){
	// 	Ember.$(document).on('mouseup','.togglebutton.opacity label',function(){

			

	// 		//$(".vectorData").toggle();

	// 		var active = Ember.$(".togglebutton label input[type=checkbox]:first-child:checked").length>0;

	// 		if (active){
	// 			Ember.$('input.value-input').val('0');
	// 			Ember.$('.noUi-origin').css('left', '0%');
	// 			Ember.$('.wmsLayer').css('opacity', '0');
	// 		}
	// 		else {
	// 			Ember.$('input.value-input').val('10');
	// 			Ember.$('.noUi-origin').css('left', '100%');
	// 			Ember.$('.wmsLayer').css('opacity', '1');
	// 		}

		// });

	}
});
