{{#if showIntro.show}}
	{{#modal-dialog close="toggleIntro" alignment="left" translucentOverlay=true container-class="project-intro"}}
    <div class="container-fluid">
        <div class="row title">
            <div class="col-md-10">
                <h1>{{project.name}}</h1>
                <h2>By: {{project.owner}}</h2>
            </div>
            <div class="col-md-2">
                <a {{action 'toggleIntro'}} class="pull-right close-button"><i class="material-icons">highlight_off</i></a>
            </div>
        </div>
        <div class="intro-body row">
        {{#if this.av}}

                <div class="intro-text col-md-6">
                    <article>
                        {{{project.intro}}}
                    </article>
                </div>
                <div class="intro-video col-md-6">
                    {{lazy-video url=project.media}}
                </div>

        {{else if a}}
            <div class="intro-text col-md-12">{{{project.intro}}}</div>
        {{else if v}}
            <div class="intro-video col-md-12">
                {{lazy-video url=project.media}}
            </div>
        {{/if}}
        </div>

        <div class="row intro-controls">
			<div class="col-md-4"></div>
            <div class="text-center explore-button col-md-4">
                <a class="btn btn-info" {{action 'toggleIntro'}}>Explore Project</a>
            </div>
            <div class="col-md-4 checkbox">
                <label class="pull-right">
					 <input type="checkbox" id="dont-show" checked={{showIntro.noIntroCookie}} onchange={{action "supressIntro"}} />
                    <span class="check"></span>
                    Don't show every time.
                </label>
            </div>
        </div>
    </div>
	{{/modal-dialog}}
{{/if}}
