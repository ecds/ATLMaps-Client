{{browse-results rasters=rasters vectors=vectors getResults='getResults'}}
{{#project-pane title='Browse Layers'}}
{{#md-card}}

      {{browse-text getResults='getResults'}}
      <hr />
      {{search-bounds getResults='getResults'}}
      <hr />
      {{browse-tags model=categories getResults='getResults'}}
      {{!-- <hr />
      {{browse-years model=yearRange getResults='getResults'}} --}}
      {{browse-institution model=institutions getResults='getResults'}}
      
{{/md-card}}
{{/project-pane}}
{{!-- <div class="row layer-heading">
    <table class="table">
        <tr>
            <td>
                <h3>Browse Layers</h3>
            </td>
            <table>
                <tr>
                    <td class="pull-right">
                        <td class="pull-right"> --}}
                            {{!
                                TODO Pulled this out of the partial. There has
                                to be a better way.
                            }}
                            {{!-- {{#link-to 'project' 'explore' class='pull-right close-button'}}
                                <i class="material-icons">highlight_off</i>
                            {{/link-to}}
                        </td>
                    </td>
                </tr>
            </table>
            <table>
                <tr>
                    <td></td>
                </tr>
            </table>
        </tr>
    </table>
</div>
<div class="browse-pane route-content">
    <div style="overflow:auto">
    {{#liquid-if editSuccess}}
        <div class="alert alert-dismissible alert-success">
            Project info has been updated!
        </div>
    {{/liquid-if}}
    {{#liquid-if editFail}}
        <div class="alert alert-dismissible alert-danger">
            Dang, something went wrong. The project info was not updated.
        </div>
    {{/liquid-if}}
    {{browse-text getResults='getResults'}}
    {{search-bounds getResults='getResults'}} --}}
    {{!-- <hr /> --}}
    {{!-- {{browse-tags model=categories getResults='getResults'}} --}}
    {{!-- <hr />
    {{browse-years model=yearRange getResults='getResults'}}
    <hr />
    {{browse-institution model=institutions getResults='getResults'}}
    </div>
</div> --}}
