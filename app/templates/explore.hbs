<div class="layer-controls">
	<div class="control-headers">
	<div class="project-title">
		<h1>{{model.name}}</h1>
	</div>
</div>
	{{ project-nav explore=true model=model rasterCount=model.raster_layer_ids.length vectorCount=model.vector_layer_ids.length }}
	{{ reorder-layers mode='explore' model=model}}

	<div class="project-info card">
		<article>Here we will say something thoughtful about how one can "explore" the layers. It will be lovely.</article>
	</div>

	<div class="vector-data vector-data-edit card">

		<div class="layer-list">
			<div class="add_layer_button">
				{{addremove-layers-modal model=model type='vector' action='initProjectUI' add='addVectorLayer' remove='removeVectorLayer'}}
			</div>
			<div class="layer-list list-group">
				{{#each model.vector_layer_ids key="@index" as |layer|}}
					<div class="list-group-item {{layer.slug}} vector-layer" id="{{layer.slug}}"  layer-id="{{layer.id}}">
					{{project-vector-icon layer=layer projectID=model.id}}
					<a {{action 'showLayerInfoDetals' layer.slug}}>{{layer.title}}</a>
					<div class="layer-info-detail {{layer.slug}}">
						<p class="list-group-item-text">{{layer.description}}</p>
						{{partial 'citation'}}

						<div class="edit-footer">
							<div class="text-right btn-group pull-right">
								<a class="btn btn-warning btn-flat btn-xs" {{action 'removeVectorLayer' layer}}><i class="material-icons">delete</i></a>
							</div>
						</div>
					</div>
					</div>

				{{else}}
					No data layers have been added.
				{{/each}}
			</div>
		</div>

	</div>
	<div class="raster-layers raster-layers-edit card">
		<div class="add_layer_button pull-left">

			{{addremove-layers-modal model=model type='raster' action='initProjectUI' add='addRasterLayer' remove='removeRasterLayer'}}

		</div>
		{{ hide-all-raster-layers }}
		<div class="layer-list list-group raster-list" id="layer_sort">
			{{#each model.raster_layer_ids key="@index" as |layer|}}
				<div class="list-group-item raster-layer {{layer.slug}}" id="{{layer.slug}}"  layer-id="{{layer.id}}">

					<span class="{{layer.data_type}} {{layer.slug}}"></span>
					<a {{action 'showLayerInfoDetals' layer.slug}}>{{layer.title}}</a>
					<span class="handle pull-right"><i class="material-icons reorder">code</i></span>
					{{opacity-slider layer=layer}}
					<div class="layer-info-detail {{layer.slug}}">

						<article class="list-group-item-text">{{layer.description}}</article>
						{{partial 'citation'}}

						<div class="edit-footer">
							<div class="text-right btn-group pull-right">
								<a class="btn btn-warning btn-flat btn-xs" {{action 'removeRasterLayer' layer}}><i class="material-icons">delete</i></a>
							</div>
						</div>

					</div>
				</div>
			{{else}}
				No scanned maps have been added.
			{{/each}}
		</div>
	</div>

	<div class="browse card">
		{{browse-layers addRaster='addRasterLayer' removeRaster='removeRasterLayer' addVector='addVectorLayer' removeVector='removeVectorLayer'}}
	</div>

	<div class="controls controls-edit card">
		<h3>Select Base Map</h3>
		{{select-base-layer mode='explore' model=model}}
	</div>

	<div class="help help card">
		<p>Help? You mean it isn't obvious?</p>
	</div>

</div>

<div class="row" id="base-map">
    {{ base-map }}
</div>

{{partial 'vector-info-window'}}
