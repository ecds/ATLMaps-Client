window.deprecationWorkflow = window.deprecationWorkflow || {};
window.deprecationWorkflow.config = {
  workflow: [
    // { handler: "silence", matchId: "ember-routing.router-resource" },
    // { handler: "silence", matchId: "ember-runtime.enumerable-contains" }
  ]
};
